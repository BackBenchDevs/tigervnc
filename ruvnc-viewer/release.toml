# cargo-release 1.x config — see: cargo release config
push-remote = "fork"
tag = true
tag-name = "v{{version}}"
tag-message = "chore: Release {{crate_name}} version {{version}}"
push = true
publish = false
sign-commit = false
sign-tag = false

# [[pre-release-replacements]]
# file = "README.md"
# search = "ruvnc-viewer v{{prev_version}}"
# replace = "ruvnc-viewer v{{version}}"
